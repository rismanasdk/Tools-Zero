#!/usr/bin/env bash

cat <<'EOF'
TCP Relay (C++ / Safe Debugging)

This module is a local traffic relay for debugging services you own.
It only allows binding to loopback addresses such as 127.0.0.1 or ::1.

1. Build C++ relay binary
2. Run relay with custom ports
3. Show relay help
EOF
